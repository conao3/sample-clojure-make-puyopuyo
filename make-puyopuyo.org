#+title: make puyopuyo
#+author: conao3

#+options: ^:nil

* Clojureプロジェクトの開始
section01からスタートします。以下の内容になっています。

tips: 以下の出力を得るoneliner。
#+begin_src sh
git ls-files | sort | bb -i -e '(doseq [l *input*] (println (str "echo '\''#+begin_src fundamental :caption " l "'\''; cat " l "; echo '\''#+end_src'\''; echo")))' | bash
#+end_src

#+begin_src clojure :caption build.clj
(ns build
  (:require
   [clojure.tools.build.api :as b]))

(def lib 'conao3/puyopuyo)
(def main 'conao3.puyopuyo)
(def version (format "0.1.%s" (b/git-count-revs nil)))
(def class-dir "target/classes")
(def jar-file (format "target/%s.jar" (namespace lib)))
(def uber-file (format "target/%s-standalone.jar" (name lib)))
(def basis (delay (b/create-basis)))

(defn jar [_]
  (b/write-pom {:class-dir class-dir
                :lib lib
                :version version
                :basis @basis
                :src-dirs ["src"]})
  (b/copy-dir {:src-dirs ["src" "resources"]
               :target-dir class-dir})
  (b/jar {:class-dir class-dir
          :jar-file jar-file}))

(defn uber [_]
  (b/copy-dir {:src-dirs ["src" "resources"]
               :target-dir class-dir})
  (b/compile-clj {:basis @basis
                  :ns-compile [main]
                  :class-dir class-dir})
  (b/uber {:class-dir class-dir
           :uber-file uber-file
           :basis @basis
           :main main}))
#+end_src

#+begin_src clojure :caption deps.edn
{:paths ["src" "resources"]
 :deps
 {org.clojure/clojure {:mvn/version "1.12.2"}
  org.clojure/tools.logging {:mvn/version "1.3.0"}
  org.slf4j/slf4j-simple {:mvn/version "2.0.17"}
  com.github.clj-easy/graal-build-time {:mvn/version "1.0.5"}}

 :aliases
 {:dev {:extra-paths ["dev" "resources-dev"]
        :extra-deps {lambdaisland/kaocha {:mvn/version "1.91.1392"}
                     com.stuartsierra/component.repl {:mvn/version "1.0.0"}}}
  :nrepl {:extra-deps {nrepl/nrepl {:mvn/version "1.4.0"}
                       cider/cider-nrepl {:mvn/version "0.57.0"}
                       cider/piggieback {:mvn/version "0.6.1"}}
         :jvm-opts ["-Djdk.attach.allowAttachSelf"]}
  :repl {:main-opts ["-m" "nrepl.cmdline"
                     "--interactive"
                     "--middleware" "[cider.nrepl/cider-middleware,cider.piggieback/wrap-cljs-repl]"]}
  :test {:main-opts ["-e" "(require 'dev)" "-m" "kaocha.runner"]}
  :build {:deps {io.github.clojure/tools.build {:mvn/version "0.10.10"}}
          :ns-default build
          :jvm-opts ["-Dclojure.compiler.direct-linking=true"]}}}
#+end_src

#+begin_src clojure :caption dev/dev.clj
(ns dev)
#+end_src

#+begin_src clojure :caption dev/user.clj
(ns user)
#+end_src

#+begin_src conf :caption .gitignore
/.cpcache
/target
#+end_src

#+begin_src makefile :caption Makefile
.PHONY: all
all: native

UNAME_OS := $(shell uname -s)

ifeq ($(UNAME_OS),Darwin)
GRAAL_BUILD_ARGS += -H:-CheckToolchain
endif

.PHONY: repl
repl:
	clj -M:dev:repl

.PHONY: update
update:
	clojure -Sdeps '{:deps {org.slf4j/slf4j-simple {:mvn/version "RELEASE"} com.github.liquidz/antq {:mvn/version "RELEASE"}}}' -M -m antq.core --upgrade --force

.PHONY: test
test:
	clojure -M:dev:test

.PHONY: uber
uber: target/puyopuyo-standalone.jar

target/puyopuyo-standalone.jar:
	clojure -T:build uber

.PHONY: native
native: target/puyopuyo

target/puyopuyo: target/puyopuyo-standalone.jar
	native-image -jar $< \
	--features=clj_easy.graal_build_time.InitClojureClasses \
	--verbose \
	--no-fallback \
	$(GRAAL_BUILD_ARGS) \
	$@

.PHONY: clean
clean:
	rm -rf target
#+end_src

#+begin_src clojure :caption src/conao3/puyopuyo.clj
(ns conao3.puyopuyo
  (:gen-class))

(defn add [a b]
  (+ a b))

(defn -main [& args]
  (println "Hello World," args))
#+end_src

#+begin_src clojure :caption test/conao3/puyopuyo_test.clj
(ns conao3.puyopuyo-test
  (:require
   [clojure.test :as t]
   [conao3.puyopuyo :as c.puyopuyo]))

(t/deftest add-test
  (t/is (= 3 (c.puyopuyo/add 1 2))))
#+end_src

#+begin_src clojure :caption tests.edn
#kaocha/v1
{}
#+end_src

* figwheelの設定
** deps.ednの追加
figwheelとreagentの依存を追加します。
React19に対応するため、alpha版を追加しました。

#+begin_src clojure
{:deps
 {...
  org.clojure/clojurescript {:mvn/version "1.12.42"}
  com.bhauman/figwheel-main {:mvn/version "0.2.20"}
  reagent/reagent {:mvn/version "2.0.0-alpha3"}}

 :alias
 {...
  :repl-cljs {:main-opts ["-m" "figwheel.main" "--build" "dev" "--repl"
                          "--middleware" "[cider.nrepl/cider-middleware,cider.piggieback/wrap-cljs-repl]"]}}}
#+end_src

** pnpmの追加
React等のnodeの依存関係を管理するツールとしてpnpmを採用します。

#+begin_src sh
pnpm init
pnpm i react react-dom
pnpm i -D webpack webpack-cli
#+end_src

pnpmは依存関係の解決が厳格なため、以下の依存も明示的に入れる必要がありました。

#+begin_src sh
pnpm i process scheduler
#+end_src

初期生成と自動編集されたpackage.jsonから少し変更して以下の形になりました。

#+begin_src js :caption package.json
{
  "name": "section02",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.18.0",
  "dependencies": {
    "process": "^0.11.10",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "scheduler": "^0.27.0"
  },
  "devDependencies": {
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1"
  }
}
#+end_src

** cljsエントリポイントの追加
cljsのエントリポイントを作成します。

#+begin_src clojure :caption src/conao3/puyopuyo/cljs/puyopuyo.cljs
(ns ^:figwheel-hooks conao3.puyopuyo.cljs.puyopuyo
  (:require [reagent.dom.client :as reagent.dom.client]))

(enable-console-print!)

(defn App []
  [:div "hello world"])

(defn ^:after-load init []
  (let [root (-> js/document (.getElementById "app") reagent.dom.client/create-root)]
    (reagent.dom.client/render root [App])))

(defonce initialized (do (init) true))
#+end_src

** index.html作成
生成されるjsを読み込むためのindex.htmlを作成します。

#+begin_src html :caption resources/public/index.html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="https://clojurescript.org/images/cljs-logo-icon-32.png">
  </head>
  <body>
    <div id="app"></div>
    <script src="/dist/js/main_bundle.js" type="text/javascript"></script>
  </body>
</html>
#+end_src

** figwheel設定ファイルの追加
figwheelの設定ファイルを追加します。

#+begin_src clojure :caption figwheel-main.edn
{:open-url false}
#+end_src

#+begin_src clojure :caption dev.cljs.edn
^{:auto-bundle :webpack}
{:main conao3.puyopuyo.cljs.puyopuyo
 :output-dir "resources-dev/public/dist/js"}
#+end_src

#+begin_src clojure :caption prd.cljs.edn
^{:auto-bundle :webpack}
{:main conao3.puyopuyo.cljs.puyopuyo
 :output-dir "resources/public/dist/js"
 :optimizations :advanced}
#+end_src

** user.cljの修正
figwheelはresourcesをpathに入れることを要求しますが、cljsのソースファイルを配置するためresourcesを自動再読み込みするとエラーになることがあります。
この設定をすることで、自動再読み込みの対象から =resources= と =resources-dev= を抜くことができます。

#+begin_src clojure
(ns user
  (:require
   [clojure.java.classpath :as cp]
   [clojure.string :as str]
   [clojure.tools.namespace.repl :as clojure.tools.namespace.repl]))

(->> (cp/classpath-directories)
     (filter #(not (str/includes? % "resources")))
     (apply clojure.tools.namespace.repl/set-refresh-dirs))
#+end_src

** figwheelの起動
Makefileに以下の内容を入れます。

#+begin_src makefile
.PHONY: repl-cljs
repl-cljs:
	clj -M:dev:nrepl:repl-cljs
#+end_src

figwheelのREPLは以下のように起動できます。
ブラウザで =localhost:9500= を開くとfigwheelのREPLが変化し、cljs.userのREPLとして利用できるようになります。

#+begin_src sh
$ make repl-cljs
clj -M:dev:nrepl:repl-cljs
[Figwheel] Validating figwheel-main.edn
[Figwheel] figwheel-main.edn is valid \(ツ)/
[Figwheel] Compiling build dev to "resources-dev/public/dist/js/main.js"
[Figwheel] Successfully compiled build dev to "resources-dev/public/dist/js/main.js" in 0.532 seconds.
[Figwheel] Bundling: npx webpack --mode=development --entry ./resources-dev/public/dist/js/main.js --output-path ./resources-dev/public/dist/js --output-filename main_bundle.js
[Figwheel] Watching paths: ("src") to compile build - dev
[Figwheel] Starting Server at http://localhost:9500
[Figwheel] Starting REPL
Prompt will show when REPL connects to evaluation environment (i.e. a REPL hosting webpage)
Figwheel Main Controls:
          (figwheel.main/stop-builds id ...)  ;; stops Figwheel autobuilder for ids
          (figwheel.main/start-builds id ...) ;; starts autobuilder focused on ids
          (figwheel.main/reset)               ;; stops, cleans, reloads config, and starts autobuilder
          (figwheel.main/build-once id ...)   ;; builds source one time
          (figwheel.main/clean id ...)        ;; deletes compiled cljs target files
          (figwheel.main/status)              ;; displays current state of system
Figwheel REPL Controls:
          (figwheel.repl/conns)               ;; displays the current connections
          (figwheel.repl/focus session-name)  ;; choose which session name to focus on
In the cljs.user ns, controls can be called without ns ie. (conns) instead of (figwheel.repl/conns)
    Docs: (doc function-name-here)
    Exit: :cljs/quit
 Results: Stored in vars *1, *2, *3, *e holds last exception object
[main] INFO org.eclipse.jetty.server.Server - jetty-11.0.24; built: 2024-08-26T18:11:22.448Z; git: 5dfc59a691b748796f922208956bd1f2794bcd16; jvm 25+37-jvmci-b01
[main] INFO org.eclipse.jetty.server.handler.ContextHandler - Started o.e.j.s.ServletContextHandler@1389257c{/,null,AVAILABLE}
[main] INFO org.eclipse.jetty.server.AbstractConnector - Started ServerConnector@b1c7b36{HTTP/1.1, (http/1.1)}{0.0.0.0:9500}
[main] INFO org.eclipse.jetty.server.Server - Started Server@378d8d1b{STARTING}[11.0.24,sto=0] @9370ms
ClojureScript 1.12.42
cljs.user=>
#+end_src

** [Stage 01] ぷよぷよを作ってみよう
(イントロダクション)

** [Stage 02] 開発環境を用意しよう
ここまででReactが動くようになっているはず。以上。

** [Stage 03] ステージを作ってみよう
*** アプリ用のエントリーポイントを作る
=puyopuyo.cljs= はアプリのエントリーポイントのため、別ファイルにアプリ用のファイルを作る。

#+begin_src clojure :caption src/conao3/puyopuyo/cljs/app.cljs
(ns conao3.puyopuyo.cljs.app)

(defn App []
  [:div "hello"])
#+end_src

そしてこのコンポーネントを利用するようにする。

#+begin_src clojure :caption src/conao3/puyopuyo/cljs/puyopuyo.cljs
(ns ^:figwheel-hooks conao3.puyopuyo.cljs.puyopuyo
  (:require
   ...
   [conao3.puyopuyo.cljs.app :as c.cljs.app]))

(defn ^:after-load init []
  (let [root (-> js/document (.getElementById "app") reagent.dom.client/create-root)]
    (reagent.dom.client/render root [c.cljs.app/App])))
#+end_src

*** ステージを作る
=config= に設定値を集める宗派のようです。

#+begin_src clojure :caption src/conao3/puyopuyo/cljs/puyopuyo.cljs
(ns conao3.puyopuyo.cljs.app)

(def config
  {:puyo-image-width 40
   :puyo-image-height 40
   :stage {:cols 6
           :rows 12
           :background-color "#11213b"}})

(defn Stage []
  [:div {:style {:width (str (* (-> config :puyo-image-width) (-> config :stage :cols)) "px")
                 :height (str (* (-> config :puyo-image-height) (-> config :stage :rows)) "px")
                 :background "url(sega-img/puyo_2bg.png)"
                 :background-color (-> config :stage :background-color)}}])
(defn App []
  [Stage])
#+end_src

[[./img/37e4c1d4-7cd0-4f8c-af63-1f31100acabb.png]]

** [Stage 04] ぷよを表示させてみよう
** [Stage 05] 自由落下させてみよう
** [Stage 06] ぷよを消してみよう
** [Stage 07] 全消しを作ってみよう
** [Stage 08] プレイヤー操作のぷよを落下させてみよう
** [Stage 09] ↓キーで落下速度を増してみよう
** [Stage 10] 左右に動かせるようにしよう
** [Stage 11] 回転させてみよう
** [Stage 12] ネクストぷよを出してみよう
** [Stage 13] スコアを表示しよう
** [Stage 14] ゲームオーバー画面を作ろう
** [Stage 15] スマートフォン対応してみよう

